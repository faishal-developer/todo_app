import TodoList from '@/components/todoList/todo.view';
import React from 'react';

const TodoPage = () => {
    return (
        <div>
            <TodoList/>
        </div>
    );
};

export default TodoPage;