export const status = {
  pending: "pending",
  completed: "completed",
  cancaled: "cancaled",
};

export const lsnames = {
  todo: "todo",
};

export type Ierror = { title?: string | null; time?: string | null };
